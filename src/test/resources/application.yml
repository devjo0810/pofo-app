spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  redis:
    ssl: false
    host: 132.226.227.191
    port: 6379
    password:

logging:
  level:
    org.hibernate.sql: debug

app:
  jwt:
    secret: test
    expire: 86400000 # ms
  file:
    upload:
      path: /upload/test